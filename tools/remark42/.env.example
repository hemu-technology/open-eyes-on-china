# Copy this file to .env and fill values. Do NOT commit the real .env
## Core
# Public base URL of your Remark42 instance
REMARK_URL=https://comments.openeyesonchina.com
# Site identifier (must match SITE passed to server)
SITE=open-eyes-on-china
# Secret key (generate a long random string). Keep private.
SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING
# Hosts allowed to embed (comma separated, no protocol)
ALLOWED_HOSTS=openeyesonchina.com,www.openeyesonchina.com

# Google OAuth (optional)
AUTH_GOOGLE_CID=
AUTH_GOOGLE_CSEC=

# Other providers (optional)
AUTH_FACEBOOK_CID=
AUTH_FACEBOOK_CSEC=
# Facebook notes:
# - Valid OAuth Redirect URI: https://comments.openeyesonchina.com/auth/facebook/callback
# - App Domains: openeyesonchina.com
# - Switch app to Live mode for non-developer users
# - Website URL (optional but recommended): https://openeyesonchina.com
AUTH_MICROSOFT_CID=
AUTH_MICROSOFT_CSEC=
AUTH_TWITTER_CID=
AUTH_TWITTER_CSEC=
## Microsoft tenant selection (required if you want personal Microsoft accounts to work)
# AUTH_MICROSOFT_TENANT controls which Microsoft audience can sign in:
# - common: allows BOTH work/school (Azure AD) and personal Microsoft accounts (Outlook/Hotmail).
#   IMPORTANT: Your Azure App Registration must have "Supported account types" set to
#   "Accounts in any organizational directory and personal Microsoft accounts".
#   Otherwise users will get: unauthorized_client: The client does not exist or is not enabled for consumers.
# - organizations: only work/school accounts (no personal Microsoft accounts).
# - consumers: only personal Microsoft accounts.
AUTH_MICROSOFT_TENANT=common
AUTH_APPLE_CID=
AUTH_APPLE_TID=
AUTH_APPLE_KID=
# If you mount the key file, keep this path as is
AUTH_APPLE_PRIVATE_KEY_FILEPATH=/srv/var/apple.p8

# Telegram (optional)
AUTH_TELEGRAM=false
TELEGRAM_TOKEN=

# Allow anonymous comments? default false
AUTH_ANON=false

## Email login (magic link) and notifications (optional)
# 1) Configure SMTP_ variables
# 2) Set AUTH_EMAIL_ENABLE=true and AUTH_EMAIL_FROM
# 3) (Optional) enable user/admin notifications via email

# SMTP connection
SMTP_HOST=
SMTP_PORT=
SMTP_TLS=false
SMTP_STARTTLS=false
SMTP_INSECURE_SKIP_VERIFY=false
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TIMEOUT=10s

# Email auth (magic link)
AUTH_EMAIL_ENABLE=false
AUTH_EMAIL_FROM=
# AUTH_EMAIL_SUBJ=remark42 confirmation
# AUTH_EMAIL_CONTENT_TYPE=text/html

# Email notifications
# Users can subscribe to replies by email
# NOTIFY_USERS=email
# NOTIFY_EMAIL_FROM=
# Admins can receive an email for each new comment
# NOTIFY_ADMINS=email
# ADMIN_SHARED_EMAIL=

# Optional anti-abuse settings for anonymous mode
# VOTES_IP=true
# ANON_VOTE=false
# RESTRICTED_NAMES=admin,moderator
